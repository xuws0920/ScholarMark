# ScholarMark PDF 文献阅读器 — 开发进展

## 当前状态：✅ 核心功能 + 公式渲染 + 目录大纲

**更新时间**：2026-02-13 15:23

---

## 已完成的工作

### 1. 项目基础搭建

- 使用 Vite + 原生 JS 构建
- 引入依赖：`pdfjs-dist`（PDF 渲染）、`marked`（Markdown 解析）、`highlight.js`（代码高亮）
- 暗色学术主题 UI 设计系统

### 2. PDF 渲染模块 (`pdf-viewer.js`)

- 基于 PDF.js 加载和渲染 PDF
- 翻页、缩放（50%-300%）、适应宽度
- 文本层实现文本选取
- 高亮标注层

### 3. 文本标注模块 (`annotator.js`)

- 选中文本弹出浮动工具栏
- 5 种标注颜色（黄、绿、蓝、紫、红）
- 标注数据持久化到 IndexedDB
- **核心：点击标注 → 跳转到关联笔记**

### 4. Markdown 笔记模块 (`note-editor.js`)

- Markdown 编辑 + 实时预览
- 一键将选中 PDF 文本插入为引用块
- 标注↔笔记双向跳转
- 多笔记管理（每个 PDF 独立）
- 自动保存
- **✨ 新增：LaTeX 数学公式渲染（KaTeX）**
  - 行内公式：`$E=mc^2$`
  - 块级公式：`$$\sum_{i=1}^n x_i$$`

### 5. 文献库管理 (`library.js`)

- PDF 导入（文件选择 + 拖放）
- 文献列表展示与切换
- 删除文献（同时清理标注和笔记）

### 6. 搜索模块 (`search.js`)

- 全文搜索标注和笔记内容
- 搜索结果分组展示
- 点击跳转到对应 PDF/标注

### 7. 导出功能 (`export.js`)

- File System Access API 选择本地目录
- 按「PDF文件名/笔记.md」结构导出
- 支持批量导出

### 8. 数据持久化 (`storage.js`)

- IndexedDB 存储 PDF、标注、笔记、设置
- 完整 CRUD 操作

### 9. ✨ PDF 目录大纲 (`outline.js`)

- 自动提取 PDF 文档目录书签
- 可折叠的树形目录结构
- 点击目录项跳转到对应页面
- 左侧栏 Tab 切换（文献库 ↔ 目录）

---

## 开发服务器

- 地址：<http://localhost:5173/>
- 启动命令：`npm run dev`

## 待优化项

- 浏览器测试工具暂不可用，需要用户手动在浏览器中测试
- 后续可根据实际使用反馈优化 UI 细节

---

## 最新修复记录

### 2026-02-20：修复双页视图原文 PDF 不显示问题

- **问题**：切换到双页（split-view）模式后，左侧原文 PDF 页面被压缩到接近 0 高度
- **根因**：`.pdf-page-wrapper` 在 flex column 布局中默认 `flex-shrink: 1`，split-view 模式下 `#pdf-pages` 高度被约束，所有页面 wrapper 被 flex 压缩
- **修复**：
  - `pdf-viewer.css`：给 `.pdf-page-wrapper` 和 `.page-number-label` 添加 `flex-shrink: 0`
  - `pdf-viewer.js`：导出 `fitWidth` 函数
  - `main.js`：切换 split-view 后调用 `fitWidth()` 重新适配宽度

### 2026-02-20：中栏双页视图可拖动调整宽度

- **功能**：split-view 模式下，PDF 原文与翻译预览之间新增可拖动分隔条
- **实现**：
  - `index.html`：在 `#pdf-pages` 和 `#pdf-translation-pane` 之间插入 `#resize-split`
  - `pdf-viewer.css`：分隔条样式（默认隐藏，split-view 下显示），翻译面板 min/max 宽度约束
  - `main.js`：`setupSplitResize()` 实现拖拽交互，面板宽度持久化到 localStorage，拖拽结束自动 `fitWidth()`

### 2026-02-27：收集 UI 美化需求与设计方案规划

- **阶段完成**：完成了前端 UI 美化的需求调研（不改变已有业务逻辑与操作流）。
- **确认风格**：
  - **整体质感**：采用类 Notion 的极简风，侧重留白与无干扰阅读体验。
  - **色彩基调**：主色调选用**科技蓝（Professional Blue）**，体现学术工具的严谨与专注。
  - **字体排版**：优先使用**无衬线体（Sans-Serif）**，搭配舒适的阅读行距。
  - **层级展现**：采用**微量层次感（Subtle Depth）**，大面积平实加上精细柔和的弥散阴影提升交互呼吸感。

### 2026-02-27：前端页面全面美化（Notion极简风）

- **功能描述**：在不修改任何 JavaScript 业务逻辑和 HTML  DOM 结构的前提下，通过纯 CSS 重构全局样式，大幅提升视觉与阅读体验。
- **改动细节**：
  - **色彩基调重构**：引入科技蓝（Science Blue `#3B82F6` 系列）作为交互状态、按钮悬停和标签激活的主强调色。暗色背景从纯黑过渡到高级灰（底色设为 `#191919`），浅色背景引入 `#F7F7F5` 区分层级。
  - **排版系统升级**：全面切换到现代无衬线体展示（以 `Inter` 为首选），阅读界面的文本行高调整致 `1.65+` 以增加呼吸感。
  - **阴影与边框控制**：引入微量层次感（Subtle Depth），隐藏多余的默认边框，仅保留极其微弱的 `1px` 分割线，对浮层组件使用高级弥散阴影 `0 12px 48px rgba(0, 0, 0, 0.12)`。
- **涉及文件**：`index.css`（全局变量重绘与 Normalize）、`sidebar.css`（扁平化侧栏状态集）、`pdf-viewer.css`（背景浮层重定）、`notes.css`（优化 Markdown 视区排版）。

### 2026-02-27：侧栏悬浮化与无界空间整合（UI Redesign Phase 2）

- **功能描述**：采用极致“保守修改”策略，在完全不改动原始 DOM 结构的情况下，利用纯 CSS 实现了组件的悬浮感与画布大空间融合。
- **改动细节**：
  - **左侧图标悬浮胶囊化**：在左侧栏收纳模式 (`left-sidebar-collapsed`) 下，将原本吸附边缘的按钮重置为一个悬浮在左侧空间的毛玻璃胶囊（`backdrop-filter: blur(16px)`），并附带柔和阴影，大幅提升现代感。
  - **右侧无边框融合**：移除了右侧所有面板和容器之间的刚性实线边框（重构为 `border: none`），仅依赖内容背景空间的色块对比切分视觉区域。
  - **选项卡极简扁平化**：右侧所有 Tab 按钮放弃了背景区块包裹，化繁为简为单纯的变色文字；激活状态仅以一条极简底部蓝线（`var(--accent-primary)`）作为指示，彻底消解了组件的厚重感。

### 2026-02-27：沉浸沉浸式阅读与防遮挡优化（UI Redesign Phase 3）

- **功能描述**：修复侧栏胶囊与目录弹窗的物理遮挡碰撞，并强化 PDF 页面的拟物立体感与画布消隐融合。
- **改动细节**：
  - **左侧胶囊避让定位**：将左侧收纳状态的悬浮胶囊垂直居中定位（`top: 50%`，配合 `Y` 轴偏移），彻底避开左上角的目录弹窗展开区，符合主流笔记软件（如 GoodNotes）的工具箱位置心智。
  - **PDF 顶栏底色褪隐**：移除了顶部阅读工具栏（放缩、页码等）的独立背景色与其下边框，使其在视觉上呈现为直接印在底层画布之上的组件。
  - **纸张材质强化**：微调了 Light 模式下大背景的底色（变为浅浅的灰 `F7F7F9`），并将纯内页 `.pdf-page` 提纯为极白，辅以加强版的弥散实体下阴影 `0 4px 24px rgba(0, 0, 0, 0.08)`，使“纸”真正浮在“桌面”上。

### 2026-02-27：左侧栏核心交互按钮拯救重组（UI Redesign Phase 4）

- **功能描述**：在绝对不修改 HTML 结构的约束下，利用 CSS 高级定位恢复了因隐藏 Sidebar Header 丢失的展开功能，并将其统一纳入悬浮胶囊之中。
- **改动细节**：
  - **原生状态保护**：明确了只在 `.left-sidebar-collapsed` 激活时改变布局，展开状态下各项间距、按钮和文献库显示100%保持原始外观（此时彻底隐藏悬浮胶囊）。
  - **结构越狱与合并**：当收纳侧栏时，将用于展开的按钮（`#btn-toggle-left-sidebar`）通过绝对定位强行“抽离”其隐藏的父元素流，并落点于刚刚扩大了 `padding-top` 的毛玻璃胶囊盒子的最上方。
  - **视觉翻转变形**：对置入胶囊的展开按钮施加变形（`transform: rotate(180deg)`），将原本向左指意的“收纳”箭头，巧妙地翻转为符合逻辑的向右拉出的展开箭头提示。

### 2026-02-27：双屏悬浮融合与顶栏色调统一（UI Redesign Phase 5）

- **功能描述**：修复双页拆分视图下主阅读区与翻译面板的视觉断层，同时统一 PDF 工具栏与系统大标题栏的背景色。
- **改动细节**：
  - **工具栏穹顶化**：撤销了 PDF 工具栏（`#pdf-toolbar`）的背景透明化，将其底色强绑定为与系统 Header 完全一致的 `var(--bg-secondary)`，保留底边框，使二者在视觉上合为一个宽大稳固的控制面板。
  - **翻译面板纸张化**：赋予右侧全文翻译面板（`#pdf-translation-pane`）与左侧 PDF 画布完全相同的极白底色（`#ffffff`）与强弥散阴影，并抹除周围生硬的实线边框。
  - **极简内边距空间**：极大地压缩了全文翻译预览区（`.pdf-translation-preview` 和 Markdown 注入区）的内边距（`padding` 降至 `4px 10px`），最大化空间利用率并在视觉上向左侧紧密的 PDF 文字列看齐。

### 2026-02-27：UI P0 问题修复（字体加载与阴影冲突）

- **修复范围**：`index.html`、`pdf-viewer.css`、`progress.md`
- **修复内容**：
  - **字体加载一致性**：在 Google Fonts 链接中新增 `Inter` 字体族，确保全局 `--font-sans` 的首选字体可被实际加载。
  - **阴影冲突清理**：移除 split-view 下翻译面板重复的 `box-shadow` 声明，保留设计目标阴影值，避免后声明覆盖前声明导致视觉不一致。
  - **冗余边框清理**：删除翻译面板中被后续 `border: none !important;` 覆盖的冗余边框声明，降低样式维护复杂度。

### 2026-02-27：收纳胶囊组拖拽与抽屉跟随锚点修复

- **问题**：收纳态点击胶囊“文献库”后，抽屉固定在左上角，未跟随按钮位置；胶囊组无法自由移动。
- **修复内容**：
  - **抽屉锚点定位**：新增抽屉定位逻辑，打开抽屉时基于 `#btn-collapsed-entry-library` 的实际位置计算 `left/top`，并在窗口缩放与胶囊拖拽过程中实时重定位。
  - **胶囊拖拽能力**：为 `#collapsed-entry-rail` 增加拖拽交互（带拖动阈值，避免误触点击），支持在主工作区范围内自由拖动。
  - **位置持久化**：胶囊拖动后将位置写入 `localStorage`，刷新后恢复；窗口尺寸变化时自动约束回可视范围。
  - **交互细节**：拖动时临时抑制按钮点击，避免拖拽结束误触发文献库/卡片库操作。

### 2026-02-27：收纳态展开按钮真实嵌入胶囊组

- **问题**：展开按钮仅通过绝对定位“看起来”在胶囊组中，拖动胶囊时按钮未跟随移动。
- **修复内容**：
  - **DOM 重挂载**：在左侧栏收纳时将 `#btn-toggle-left-sidebar` 真实移动到 `#collapsed-entry-rail` 内；展开时再移回 `.sidebar-header-actions`。
  - **样式收敛**：移除收纳态下对展开按钮的绝对定位方案，改为胶囊组内普通流式布局，确保拖拽时按钮与胶囊整体同步移动。

### 2026-02-28：学术演进图谱连线优化

- **问题**：图谱中文献节点间的连线箭头被目标节点遮挡，路径绕弯过多不够简洁。
- **修复内容**：
  - **箭头遮挡修复**：G6 节点配置新增 `anchorPoints`，将边端点移到节点边缘外侧 4px（`ARROW_OFFSET`），让箭头完整可见且几乎贴合节点。搭配 `getGraphAnchorPointFromNode` 同步偏移，确保控制点计算与 G6 渲染一致。
  - **自动重算旧边路由**：`normalizeEdge` 改为对所有非手动路由的边强制重新推算锚点方向和控制点路径，不再使用数据库中存储的旧数据。
  - **锚点方向优化**：`inferNodeAnchorFromDirection` 增加对水平连接的偏好（当水平距离超过节点宽度 30% 时优先选择左右方向），减少不必要的上下连接。
  - **路由评分优化**：`chooseBestOrthogonalRoute` 采用加权评分（拐弯权重 200 + 路径长度），偏好更短更简洁的路径。`EDGE_STUB` 从 34px 减至 22px 减少伸出段。
- **涉及文件**：`src/modules/graph-workspace.js`
